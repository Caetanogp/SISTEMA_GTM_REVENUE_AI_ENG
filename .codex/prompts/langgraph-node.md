# langgraph-node

Add or change a LangGraph node, edge, interrupt or checkpoint. Use when modifying the agent graph, prompts, routing or the human-in-the-loop resume path.

Read `docs/playbooks/langgraph-node.md` and follow it. That playbook is shared with Claude Code
(`.claude/skills/langgraph-node/SKILL.md`) — edit the playbook, never the wrapper.

Project rules that always apply: `AGENTS.md`.
