# api-endpoint

Add a FastAPI endpoint with auth, RBAC, tenant isolation, error mapping and integration tests. Use when exposing a use case over HTTP.

Read `docs/playbooks/api-endpoint.md` and follow it. That playbook is shared with Claude Code
(`.claude/skills/api-endpoint/SKILL.md`) — edit the playbook, never the wrapper.

Project rules that always apply: `AGENTS.md`.
