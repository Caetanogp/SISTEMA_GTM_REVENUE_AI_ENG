# eval-case

Add a case to a golden dataset or write a scorer, and keep the eval gate honest. Use when changing prompts, the graph, retrieval or a tool contract, or after a real failure.

Read `docs/playbooks/eval-case.md` and follow it. That playbook is shared with Claude Code
(`.claude/skills/eval-case/SKILL.md`) — edit the playbook, never the wrapper.

Project rules that always apply: `AGENTS.md`.
