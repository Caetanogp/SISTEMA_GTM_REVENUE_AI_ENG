# techdebt

End-of-session sweep for duplication, layer drift, dead code, silent failures and stale docs. Use before writing the handoff.

Read `docs/playbooks/techdebt.md` and follow it. That playbook is shared with Claude Code
(`.claude/skills/techdebt/SKILL.md`) — edit the playbook, never the wrapper.

Project rules that always apply: `AGENTS.md`.
