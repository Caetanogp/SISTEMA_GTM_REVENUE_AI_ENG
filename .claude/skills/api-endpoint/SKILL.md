---
name: api-endpoint
description: Add a FastAPI endpoint with auth, RBAC, tenant isolation, error mapping and integration tests. Use when exposing a use case over HTTP.
---

# api-endpoint

Follow the procedure in `docs/playbooks/api-endpoint.md` — read that file now and apply it.

It is the single source of truth for this workflow and is shared with Codex through
`.codex/prompts/api-endpoint.md`. If the procedure needs to change, edit the playbook, not this wrapper.

Project rules that always apply: `AGENTS.md`.
