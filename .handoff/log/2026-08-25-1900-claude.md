---
agent: claude-code
updated_at: 2026-08-25
branch: develop
spec: SPEC-001-vertical-slice-account-prioritization
phase: "0 done — next is phase 1, code-first MVP"
status: ready-to-implement
---

# Current state

## Goal

Ship the SPEC-001 vertical slice end to end: natural-language request → CRM context → read tool →
reasoning → proposed action → HITL approval → write tool → audit trail.

## Now

Nothing in flight. The engineering foundation is committed on `develop`; SPEC-001 is written and
not started. **Create a feature branch before the first line of code.**

## Done

- Engineering foundation, commit `d423f9f` on `develop` (115 files). Clean Architecture tree under
  `packages/core/revops/` + `apps/{api,worker,mcp,web}`.
- `AGENTS.md` canonical (169 lines) + `CLAUDE.md` (63 lines, `@AGENTS.md`).
  Evidence: `python scripts/check_agent_docs.py` → passed.
- Layer contracts in `pyproject.toml` (4 import-linter contracts) + `tests/architecture/`.
  Evidence: `pytest tests/ -q` → 1 passed, 1 skipped (import-linter not installed yet).
- `ruff check .` → All checks passed; `ruff format --check .` → 19 files already formatted.
- Security hooks verified by hand:
  `block_sensitive_files.py` blocks `.env` (exit 2), allows `.env.example` and normal files;
  `enforce_branch_policy.py` blocks `git commit` and `git push --force` on `develop` (exit 2),
  allows them on `feature/*`.
- No secrets tracked: `git ls-files | grep -iE "env|secret|credential"` → only `.env.example`.
- Config syntax validated: `docker compose config` valid; all 4 workflows, pre-commit,
  `pyproject.toml`, `.claude/settings.json`, `.codex/config.toml` parse cleanly.
- 12 playbooks → 12 Claude skills + 14 Codex prompts; 8 subagents; 4 slash commands.
- `docs/tooling/RESEARCH.md`: MCP/plugin dossier. **Nothing installed.**

## Next

1. `git checkout -b feature/SPEC-001-domain` — never work on `develop`.
2. Install the toolchain: `pip install -e ".[dev]"`, then confirm `mypy .`, `lint-imports` and
   `pytest tests/ -q` all pass (the architecture test currently skips because import-linter is
   missing). Run `pre-commit install`.
3. Domain layer for SPEC-001: `Score`, `RiskLevel`, `EmailAddress`, `Domain` value objects and the
   `Account`/`Contact`/`Opportunity` entities — pure Python, unit-tested.
4. `policies/prioritization.py` and `policies/risk.py` with unit tests on the boundaries.
5. Application ports + `PrioritizeAccounts` use case against in-memory fakes.

Full checklist: `docs/specs/SPEC-001-vertical-slice-account-prioritization/tasks.md`.

## Gotchas

- **Docker Desktop was not running**, so `docker compose up -d` was never executed end to end. The
  compose file parses (`docker compose config`), but the pgvector extension has not been verified
  live yet — do that as part of step 2.
- `mypy`, `import-linter`, `uv`, `pre-commit` and `gitleaks` are not installed on this machine.
  `ruff`, `pytest`, `python 3.12` and `docker` are.
- The domain layer must not import Pydantic — dataclasses only. Two guards: import-linter, and a
  textual test that runs even without it installed.
- `main` and `develop` are protected. A hook blocks agent commits there; the only exception is a
  commit touching nothing but `.handoff/`.
- The project guide PDF is the product source of truth, but ADR-0001 supersedes its section 16
  repository layout.
- Windows host: use Git Bash. Heredocs containing apostrophes break in this shell — write files with
  the editor tool instead.

## Resume

```bash
cd "SISTEMA_PORTFOLIO_AI_ENG"
git checkout develop && git rev-parse --abbrev-ref HEAD
git checkout -b feature/SPEC-001-domain
docker compose up -d
pip install -e ".[dev]" && pre-commit install
cat docs/specs/SPEC-001-vertical-slice-account-prioritization/tasks.md
```

## Open questions

- Which MCP servers and plugins from `docs/tooling/RESEARCH.md` to install? Recommended first:
  Context7, `frontend-design`, `superpowers`. Nothing installed so far, by design.
- Provider keys are not configured (`.env` does not exist). Needed before the graph can run against
  a real model — the fake gateway covers tests and CI until then.
- The support material (tip images and txt files) is still in the repository root, untracked,
  pending the user's confirmation to delete. Content already preserved in
  `docs/playbooks/claude-code-workflow.md` and `docs/tooling/RESEARCH.md`.
